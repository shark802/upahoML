# Core Data Science Libraries (required - ~150MB)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Database (~5MB)
mysql-connector-python>=8.1.0

# Time Series - ARIMA only (~50MB)
# Removed pmdarima (auto_arima) - use manual ARIMA parameters instead
statsmodels>=0.14.0

# REMOVED to reduce slug size:
# - xgboost>=2.0.0 (~100MB) - Code uses Random Forest/Gradient Boosting instead
# - pmdarima>=2.0.0 (~50MB) - Use manual ARIMA parameters
# - prophet>=1.1.0 (~200MB) - Code uses ARIMA instead
# - tensorflow>=2.13.0 (~500MB) - LSTM not available, code handles gracefully

# Web Framework (~10MB)
gunicorn>=21.2.0
flask>=2.3.0
flask-cors>=4.0.0

# Utilities (~1MB)
python-dateutil>=2.8.0

